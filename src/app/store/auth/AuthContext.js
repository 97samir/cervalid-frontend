// Define el contexto global de autenticación usando 
// React Context API: Permite compartir datos en toda la aplicación

import { createContext } from "react";

const AuthContext = createContext();

export default AuthContext;